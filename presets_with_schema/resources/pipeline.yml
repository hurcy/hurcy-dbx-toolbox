resources:
  pipelines:
    example_pipeline:
      name: "Example of a pipeline with a schema"

      catalog: ${resources.schemas.system1.catalog_name}
      target: ${resources.schemas.system1.name}

      libraries:
        - file:
            path: ../src/range.sql
        - file:
            path: ../src/double.sql

      serverless: true
